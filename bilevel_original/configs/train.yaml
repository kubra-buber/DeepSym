name: <model_name>
dataset_name: <dataset_name> 

hidden_dim: 128
latent_dim: 1
n_hidden_layers: 2
n_attention_heads: 3

# Bottleneck type
#   sigmoid: false -- Gumbel-Sigmoid (symbolic model, for rule extraction)
#   sigmoid: true -- plain Sigmoid   (dynamics model, for forward prediction)
sigmoid: false 

epoch: 4000
batch_size: 128
lr: 0.0001
devices:
  - 0